# Instagram notes

Instagram has the strongest rate limits of all the services we support. We are using residential proxies to get around that, but those are expensive and are only used as a last resort. 

The sidecar strategy is another implementation in Python, that runs on raspberry pi's at various homes. They manage to get some requests through, but get rate limited quickly.