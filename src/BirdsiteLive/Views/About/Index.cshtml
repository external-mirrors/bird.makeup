@model BirdsiteLive.Services.CachedStatistics
@{
    ViewData["Title"] = "About";
    // following @Model.TwitterUsers (high-priority) users
}

<div class="col-12 col-sm-12 col-md-10 col-lg-8 mx-auto">
    <section class="surface-card text-center">
        <p class="section-eyebrow">Instance</p>
        <h1 class="section-title section-title--compact">Service load</h1>
        <p class="metric-value">@Model.FediverseUsers</p>
        <p class="metric-label">fediverse users are currently connected</p>
    </section>
</div>
